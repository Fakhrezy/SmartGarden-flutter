import 'package:flutter/material.dart';

class GrafikHamaPage extends StatelessWidget {
  const GrafikHamaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Grafik Hama'),
      ),
      body: const Center(
        child: Text('Tampilan Grafik Hama'),
      ),
    );
  }
}
